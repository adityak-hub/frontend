--HOISTING
.only declaration is hoisted;
/value is not hoisted.


1. var ki hoisting 
console.log(a); // undefined
var a = 10;


---behind the scene
var a;          // hoisted
console.log(a); // undefined
a = 10;
--


.var hoist hota hai
.Automatically undefined assign ho jata hai
.Isliye error nahi aata

2. let ki hoisting
console.log(b); // ❌ ReferenceError
let b = 20;

--------
👉 Behind the scenes:

.b hoist hota hai
.BUT initialize nahi hota

✔️ Isliye error aata hai
-------


3.const ki hoisting

console.log(c); // ❌ ReferenceError
const c = 30;


----------
👉 Same as let

.hoist hota hai
.initialize nahi hota

-----------------





1.Reference error=when we access variable without initialize and declare(“variable exist hi nahi karti”)

-----------
2.TDZ = The time between variable hoisting and initialization(“variable exist karti hai but use nahi kar sakte abhi”)
TDZ is the time when a variable is declared but cannot be accessed before initialization, resulting in a ReferenceError.

{
  console.log(x); // ❌ TDZ
  let x = 5;
}

👉 Timeline samajh:

1.Scope start hua {
2.x memory me aa gaya (hoisted)
3.But value assign nahi hui
4.Is duration ko bolte hain 👉 TDZ
5.let x = 5 → TDZ khatam







Scope: scope determines the accessibility of variables,objects,functions from different parts of the code,
1.function scope.
2.Block scope.
3.lexical scope.
4.global scope.


1. function scope=variables defined inside a function are not accessible(visible) fro outside the fuunction.


ex:1---

let sum = 54; // gobal scope.   

function calsum(a,b){
    let sum= a+b; //function Scope
    console.log(sum)  --3
}

calsum(1,2);
console.log(sum);  --54

jab global scope and function scope aapas mai priority ke liye fight karte hai toh function ke andar priority ke liye function scope jitega and global ki priority ke liye global scope jitega.



2.  Block Scope = Variables declare inside a {} block cannot be accessed from outside the block,

* var pe kabhi block scope apply nhi jota. sirf let and const pe hota hai, isliye var use karna bad idea hota hai.

ex--
{
    var a = 25; 
}

console.log(a);  // print ho jaayega 


{
    let a = 25; 
}

console.log(a); // it will show an error;


for(let i=1; i<=5; i++){
    console.log(i); //block scope;
}



3.Lexical Scope=a varible defined outside a function can be accessible inside another function defined after the variable declaration.

// we use it in nested function concept.

ex----

function outerFunc(){
    let x = 5;
    let y= 6;
    function innerFunc(){   
        console.log(x);
    }
    innerFunc();
}


outerFunc();




..The opposite is NOT true.

ex----

function outerFunc(){
    let x = 5;
    let y= 6;
    function innerFunc(){   
        let a = 10;
        console.log(x);
        console.log(y);
    }
    console.log(a); // this opposite not true.
    innerFunc();
}


outerFunc();


ex---
function outerFunc(){
    let x = 5;
    let y= 6;
    function innerFunc(){    // function scope
        let a = 10;
        console.log(x);
        console.log(y);
    }
}
    
innerFunc();   // it will shown an error bcz innerFunc function scope ke under hai






******* FUNCTION

function printName(){
    console.log("aditya);
}

printName();






*****FUNCTION EXPRESSION OR NAMELESS FUNCTION

let sum = function(a,b){
    return a+b;
}
console.log(sum(5,5));



******HIGH ORDER FUNCTION
A function that does one or both:

. takes one or multiple functions as arguments
. return a function



ex.-- for  takes one or multiple functions as arguments


function multigreet(func,n){
    for(let i=1; i<=n; i++){
        func();
    }
}

let greet = function(){
    console.log("hello");

}

multigreet(greet,10);    // hum direct function bhi likh ke pass kr skte hai as arguement like-- multigreet(function() {console.log("namstey)},10);



--ex for return a function.


function oddorevenfactory(request){
    if(request=="odd"){
        let odd = function(n){
            console.log(!(n%2==0));
            
        }
        return odd;
    }else if(request=="even"){
        let even = function(n){
            console.log((n%2==0));
        }
        return even;
    }else{
        console.log("wrong request");
    }
    
}


let request = "even"
let func = oddorevenfactory("odd");   //ye ek function return krega usse func mai store kr rhe hai taaki usmai value daal kr call kr ske.
func(4);






*******METHODS
--Actions that can be performed on an objects.

const calculator={
    add: function(a,b){
        return a+b;
    },
    sub: function(a,b){
        return a-b;
    },
    mul: function(a,b){
        return a*b;
    }
}


console.log(calculator.add(1,3));



***METHOD SHORTHAND

const calculator={
    add(a,b){
        return a+b;
    },
    sub(a,b){
        return a-b;
    },
    mul(a,b){
        return a*b;
    }
}
console.log(calculator.add(1,3));


*******this keyword
"this" keyword refers to an object that is executing the current piece of code.

ex--

const student ={
    name : "aditya",
    age : 23,
    eng :98,
    math :90,
    phy:99,
    getavg(){
        let  avg = (this.eng+this.math+this.phy)/3 ;
        console.log(`${this.name} got avg marks = ${avg}`);
    }
}

student.getavg();


*** try & catch
The try statement allows you to define a block of code to be tested for errors while it is being executed.

the catch statement allows you to define a block of code to be executed, if an error occurs inthe try block.



console.log("hello world");
console.log("hello world");
console.log("hello world");
console.log("hello world");
// let a =5;
try{
    console.log(a);
}catch(err){
    console.log("caught an error.. a is not defined");
    console.log(err);
}
console.log("hello world2");
console.log("hello world2");
console.log("hello world2");
console.log("hello world2");



*****arrow functions


const sum  = (a,b) =>{
    console.log(a+b);
}

sum(2,5);

const cube = n=>{
    return(n*n*n);

}

let result =console.log(cube(3));


*** implicit(automatic) return of arrow function

const cube = n=>(
    n*n*n    

); // parenthesis lgao ya na lago.

let result =console.log(cube(3));



*****Set Timeout 
Set Timeout is a inbuilt function off window object.

ex1--
console.log("hiii Students");

setTimeout(()=>{
    console.log("Gla University");
},3000);

console.log("welcome to");




ex2--

console.log("hlo world");

const func = ()=>{
    console.log("here")
};



setTimeout(func, 2000);

console.log("aligarh")



******setInterval

after interval this piece of code continues will run.
it will not stop until we do not use clearInterval(id);

ex.1--- for unstop
console.log("My name is");
setInterval(()=>{
    console.log("from apna college");
},2000);
console.log("aditya kumar");



ex2. --- for stop


console.log("My name is");
let id1=setInterval(()=>{
    console.log("from apna college");
},2000);
console.log("aditya kumar");


clearInterval(id1);



******this with arrow functions and normal function

normal function = function mai this calling object pe depend krta hai, konsi objects se  function ko call kiya gya hai.
arrow function =  arrow function normal function ki trah nhi dekhte ki hme kisne call kiya hai woh dekhte hai ki hmare parents ko kis object ne  call kiya hai. arrow function ke liye scope whi hoga jo parent ke liye hoga.  jo parent function ko call krta hai whi arrow function inherit kr leta hai.
lexical scope ka matlab parent ka kya scope hai.


const student ={
    name :"aman",
    marks :95,
    prop : this,   //global scope
    getname :function(){
        console.log(this);
        return this.name;
    },
    getmarks:() =>{
        console.log(this);  // parent's scope
        return this.marks;
    },
    getinfo1: function(){
        setTimeout(() => {
            console.log(this);  //student
            
        },2000);
    },
    getinfo2:function(){
        setTimeout(function()  {
            console.log(this);  //window
        }, 2000);
    }
}


***callback
A callback is a function passed as argument to be executed later.(ek function jo kisi dusre function ke argument ke form mai diya jaata hai aur baad mai call hota hai to usse callback function khte hai)
ex---
function hello(callback) {
    console.log("Hello");
    callback();
}

function bye() {
    console.log("Bye");
}

hello(bye);

Step-by-step:
hello(bye) call hua
console.log("Hello") run hua
👉 Output: Hello
callback() run hua
👉 ye actually bye() hai
bye() execute hua
👉 Output: Bye

output--
Hello
Bye


ex---
function processStudent(name, callback) {
    console.log("Processing student:", name);
    callback(name);
}

function showResult(name) {
    console.log(name + " has passed");
}

processStudent("Aditya", showResult);







*****some array METHODS
arr.forEach(some function definition or name);


1. forEach:- arr.forEach(some function definition or name);

ex-- 

let arr = [1,2,3,4,5];

let print = function(el){   
    console.log(el);
}

arr.forEach(print);   // we pass function name


ex--   

let arr = [1,2,3,4,5];


arr.forEach(function(el){
    console.log(el);     // pass function definition in normal function
});


ex--   

let arr = [1,2,3,4,5];


arr.forEach((el)=>{
    console.log(el);     // pass function definition in arrow function
});




2.map
 let newArr = arr.map(some function definition or name);
* newArr has the same size as old arr.

ex---
let num = [1,2,3,4];

let double = num.map((el)=>{
    return el*2;
});


console.log(double);


3.filter:
let newArr = arr.filter(some function definition or name);
*jab callback function output mai true deta hai tab hum el ko newArr mai add kr dete hai agr false toh nhi add krte

let nums = [1,2,3,4,7,8,2,9,10,12,11];

let ans = nums.filter((el)=>{
    return el%2==0;  //even
})

console.log(ans);

4.Every:

Returns true if every element of array gives true for some function.Else returns false.

arr.every(some function definition or name);

a.
console.log([2,4,6].every((el) => el%2==0)
);
-- true


b.
console.log([2,4,6,5].every((el) => el%2==0)
);

--false


5.Some:

Returns true if some element of array gives true for some function.Else returns false.

arr.some(some function definition or name);

a.
console.log([1,2,3,4].some((el) => el%2==0)
);
-- true


b.
console.log([1,3].some((el) => el%2==0)
);

--false

6.reduce
Reduces the array to a single value

arr.reduce(reducer function with 2 variables for(accumulator, element));

a.
console.log([1,2,3,4,5].reduce((res,el)=>(res+el)));

b.
let num = [1,2,3];

let result=num.reduce((value,el)=>(value*el));


***Spread
expands an iterable into mltiple values.
ye dono ke liye kaam krta hai array ,string ye bhut jaldi se iterate karta hai as a loop.

exs--
let arr = [2,4,5,7,1,2,4,9,8,4,7,6,1,2,0,-1];

console.log(Math.min(...arr));

console.log(..."apna college");


**Spread with array literals

exs--
let arr = [1,2,1,-1];

let newArr = [...arr];
console.log(newArr);


let chars = [..."hello"];
console.log(chars);


let odd = [1,3,5,7,9];
let even = [2,4,6,8,10];

console.log(...odd , ...even);


****Spread with object literals

exs--


let data = {
    email:"kaditya41057@gmail.com",
    password : 1234
}
let newdata = {...data, id: 123,country:"India"};
console.log(newdata);



let nums = [1,2,4,7,9,3,1];
let obj1 = {...nums};
console.log(obj1);



let obj2 = {..."Gla University"};
console.log(obj2);



**Rest
rest is opposite of spread
Allows a function to take and indefinte number of arguments and bundle them in an array 

ex--
function sum(...args){   // using rest
    for(let i=0; i<args.length; i++){
        console.log("you gave us: ",args[i]);
    }
}
sum(1,7,5,0,2,-1,9);



function sum(...args){
    return args.reduce((add,el)=>(add+el));
}
console.log(sum(1,2,3,4,5));




****Destructuring
storing values of array into multiple variables

ex--
let names=["tony","steve","peter","helen","abc","uf","aaa"];

let [winner, runnerup,] = names ;   // it not an array it is individual varible.
console.log(winner);
console.log(runnerup);


*** Destructuring(Objects)




const student = {
    name:"karan",
    age:14,
    class:9,
    subjects:["hindi","sanskrit","java","c++"],
    username:"karan@123",
    password:"abcd"
}


// let {username,password} = student ;

let {username:user,password:secret} = student ;  // hum key ko match kra ke kiye new varible mai value store kra skte hai.


// console.log(username);
// console.log(password);
console.log(user);
console.log(secret);

------------------------------------------------------------------------
DOM(Document Object Model)
The DOM represents a document with a logical tree

It allows us to manipulate/change webpage content(HTML elements).


****document
.

🔹 document kya hota hai?

👉 document = poora HTML page (webpage) ka object

👉 Browser tumhari HTML file ko ek object bana deta hai
👉 Usko hi bolte hain document

🔹 Simple samajh

👉 Tumhari website = HTML page
👉 JavaScript usko access karta hai → document ke through

🔹 Example

HTML:
<img id="mainImg" src="image.jpg">

JavaScript:
document.getElementById("mainImg");

👉 Yaha:

document = poora page
getElementById = ek method
"mainImg" = element ka id

👉 Result:
✔ Ye image element mil jayega




---------
console.dir() kya hota hai?

👉 console.dir() ek method hai jo object ko detail mein dikhata hai.
object ka pura structure (properties + methods) dekhna.




***********Selecting elements byId,ByClassName,ByTagNmae

1.getElementById: returns the element as an object or null(if not found).

2.getElementsByClassName: returns the elements as an HTML collection or empty collection(if not found).

3.getElementsByTagName:returns the elements as an HTML collection or empty collection(if not found).


************Selecting elements by Query Selectors
Allows us to use any css Selectors

document.querySelector('p'); // selects first p element
document.querySelector('#myId'); // selects first element with id = myId
document.querySelector('.myClass'); // selects first element with class = myClass

document.querySelectorAll("p"); // selects all p elements by adding All in querySelector


***********manipulate the html Elements using properties & methods  

innerText
shows the visible text contained in a node.(screen pe jo dekhta hai whi dekhata hai )

textContent
shows all the full text.(kuch bhi hidden hai ya humne uski visibilty hta di hai woh bhi dikh deta hai kyoki ye apna content seedha html se uthata hai)

innerHTML
shows the full markup.(ye pura html markup utha kr dikha deta hai ex tags,p,div, spaces,etc).


******manipulating attributes

obj.getAttribute(att)   // attribute ki value pta krna
obj.setAttribute(att,val)    // attribute ki value bhi change kr skte hai
they are called getter and setter(getter and setter do functions ka set hota hai jisse value get bhi kar skte hai aur set bhi ).




*******manipulating style
style property

obj.style

a.
let heading = document.querySelector('h1');
heading.style.color='green';
heading.style.backgroundColor='yellow';


b.
let links = document.querySelectorAll(".box a");


for(let i=0; i<links.length; i++){
    links[i].Style.color="pink";
}
OR

for(link of links){
    links.style.color = "pink";
}

*****manipulating style
using classList

obj.classList

classList.add()  // to add new classes
classList.remove()  // to remove classes
classList.contains()  // to check if class exists
classList.toggle()  // to toggle between add & remove(class hogi to hta dega nhi hai to bna dega aur true and false de dega respectively)


*****navigation(we track our elements)

1.parentElement
2.children
3.previousElementSibling/nextElementSibling



*********adding elements
document.createElement('p')


1.appendChild(element)
2.append(element)
3.prepend(element)
4.insertAdjacent(where,element)



*********Removing elements

1.removeChild(element)
2.remove(element)

******DOM Events
events are signals that something has occured.(user inputs/actions).
(events signals hote hai jo btate hai ki page par kya activity hui ha ex- kya koi buttin press hua hai ya kuch aur).

1. onclick(when  an elements is clicked)

ex1---

 let btn = document.querySelector("button");
 console.dir(btn);

 btn.onclick = function(){
     prompt("button was clicked");
}



ex2--
let btns = document.querySelectorAll("button");

for(btn of btns){
    btn.onclick = sayhello;

} 


function sayhello(){
    alert("hello world!");
}


2.onmouseenter(when mouse enters an element).

ex---
let btns = document.querySelectorAll("button");

for(btn of btns){
    btn.onclick = sayhello;
    btn.onmouseenter = function(){
        console.log("you entered a button");
    }

} 

 
function sayhello(){
    alert("hello world!");
}



*******Event Listener(event Listener ki jaroorat kyo padi bcz onclick ya onmouseenter se hum sirf ek value set kr skte hai,agr hum chate hai ek click pe multiple function ho wo nhi ho paayegi that why we use evenListener)

addEventListener

element.addEventListener(event,callback)


ex----
let btns = document.querySelectorAll("button");

for(btn of btns){
    // btn.addEventListener("click",sayhello);
    // btn.addEventListener("click",sayNmae);
    btn.addEventListener("dblclick",function(){
        console.log("you clicked double")
    });

} 


function sayhello(){
    alert("hello world!");
}

function sayNmae(){
    alert("Gla university!");
}


**************'this' in eventListener

ex-- let btn = document.querySelector("button");
btn.addEventListener("click",function(){
    console.log(this);
    //console.dir(this.innerText);
});

->(jo hmara this hai woh addEventListener ke method ke callback ya function e andar jo this hai woh hmara whi cheej hai jiske liye humne eventListener create kiya hai,basically jo hmara btn hai whi this hai.[matlab this button ko specify kr rha hai or this ki help se hum btn ki property acces kr skte hai]).



***************default argument evenListener

let btn = document.querySelector("button");

btn.addEventListener("click",function(event){   //event or ev or e 
    console.log(event);
    console.log("button clicked");
})

*****keyboard event
ex--
1.
let inp = document.querySelector("input");

inp.addEventListener("keydown",function(){
    console.log("key was pressed");
});


2.
let inp = document.querySelector("input");

inp.addEventListener("keyup",function(event){
    console.log("key = ",event.key);
    console.log("code= ",event.code);
    console.log("key was relased");
});



*********Form Events
ex--

let frm = document.querySelector("form");

frm.addEventListener("submit",function(event){
    event.preventDefault();   // default cheejo ko rokne ke liye jaise action url pe ja rha tha submit ke baad.
    alert("form submitted");
});



*****extracting form data


ex---
let frm = document.querySelector("form");

frm.addEventListener("submit",function(event){
    event.preventDefault();
  let user= document.querySelector("#user");   // OR let user= this.elements[0];  OR frm.elements[0];
  let pass= document.querySelector("#pass");   // OR let user= this.elements[1];  OR frm.elements[1];
  console.dir(user.value);
  console.log(pass.value);

  alert(`hii ${user.value}, your password is ${pass.value}`);

});

**************More Events


1.change event
The change event occurs when the value of an element has been changed(only works on<input>,<textarea>,and <select>elements).

ex--
let frm = document.querySelector("form");

frm.addEventListener("submit",function(event){
    event.preventDefault();
});

let user = document.querySelector("#user");

user.addEventListener("change",function(){
    console.log("change event");
    console.log("final value is = ", this.value);
})



2.input event(jab hum chate hai ki chote chote changes track ho then we use input event).

The input event fires when the value of an <input>,<select>,or<textarea> element has been changed.


let frm = document.querySelector("form");

frm.addEventListener("submit",function(event){
    event.preventDefault();
});

let user = document.querySelector("#user");

user.addEventListener("change",function(){
    console.log("change event");
    console.log("final value is = ", this.value);
})
user.addEventListener("input",function(){
    console.log("input event"); // chota chnage bhi hoga woh bhi track hoga  but change input mai final value track hoga.
    console.log("final value is = ", this.value);
})



*****example of text editor

let inp = document.querySelector("#text");
let p = document.querySelector("p");

inp.addEventListener("input",function(){
    console.log(inp.value);
    p.innerText=inp.value;
});




***********event bubbling
jab hum html mai  different nested element ke liye alag alag event listener create krte hai use event bubblinng khte hai


ex---
let div = document.querySelector("div");
let ul = document.querySelector("ul");
let lis = document.querySelectorAll("li");


div.addEventListener("click",function(){
    console.log("div was clicked");
})

ul.addEventListener("click",function(){
    console.log("ul was clicked");
})


for(li of lis ){
    li.addEventListener("click",function(){
        console.log("li was clicked");
    });
}


(jab hum nested element ke eventListener ko triger krte hai to uske parent aur uske upper wale elements ke bhi eventListener automatic triger ho jaate hai).


****event bubbling ko rokhe kaise(by event.stopPropagation())
Event Bubbling ka matlab hai:
👉 Jab kisi child element par event hota hai, to wo upar parent elements tak travel karta hai


let div = document.querySelector("div");
let ul = document.querySelector("ul");
let lis = document.querySelectorAll("li");


div.addEventListener("click",function(){
    console.log("div was clicked");
})

ul.addEventListener("click",function(event){
    event.stopPropagation();
    console.log("ul was clicked");
})


for(li of lis ){
    li.addEventListener("click",function(event){
        event.stopPropagation();
        console.log("li was clicked");
    });
}

(ab jab nested element ke eventLiistener ko triger(click) krenege toh parent or uske upper ke eventListner triger nhi honge).




****************Todo task mai ek problem


let btn =document.querySelector("button");
let ul = document.querySelector("ul");
let inp = document.querySelector("input");


btn.addEventListener("click",function(){
    let item = document.createElement("li");
    item.innerText = inp.value;
    let delbtn = document.createElement("button");
    delbtn.innerText="delete";
    delbtn.classList.add("delete");
    item.appendChild(delbtn);
    ul.appendChild(item);
    inp.value="";   //reset to empty String(input mai task add hone ke baad hat jaye)

})


let delBtns = document.querySelectorAll(".delete");

for(delBtn of delBtns){
    delBtn.addEventListener("click",function(){
        let par =this.parentElement;
        par.remove();


    })
}

/*ismai ek problem hai ki jo element or todo kaam(sleep , eat) exist krte hai phle se ye sirf unpe kaam krta hai jo event listner se task bnaya hai, kaam nhi karega woh sirf existing element ke liye kaam krta hai
already exist kaam hai unpe hi delte btn wala eventListner kaam krega aur unpe kaam nhi krta jo hmne eventListner lga ke task add kiye due to   */


/*
⚠️ Problem kya hai?

👉 Delete button ka event sirf jo elements already page par the unpe kaam kar raha hai
👉 Jo tu baad me JS se add kar raha hai (new tasks), unpe kaam nahi kar raha

🧠 Reason (main point):

Event listener sirf un elements par lagta hai jo us time exist karte hain

Matlab:

let btns = document.querySelectorAll(".delete");

👉 Ye sirf initial buttons ko select karega
👉 Baad me jo naye buttons add hue → unpe event laga hi nahi

🔥 Isko kya bolte hain?

👉 Ye issue hota hai DOM loading + static selection ki wajah se
👉 Solution hai: Event Delegation
*/


******🔥 Event Delegation kya hai?

Event Delegation ek technique hai jisme tum
👉 child elements par event lagane ke bajaye parent par event lagate ho
aur parent hi sab child events handle karta hai






**************************//   Todo with DOM loading or static selection  solution

let btn =document.querySelector("button");
let ul = document.querySelector("ul");
let inp = document.querySelector("input");


btn.addEventListener("click",function(){
    let item = document.createElement("li");
    item.innerText = inp.value;
    let delbtn = document.createElement("button");
    delbtn.innerText="delete";
    delbtn.classList.add("delete");
    item.appendChild(delbtn);
    ul.appendChild(item);
    inp.value="";   //reset to empty String(input mai task add hone ke baad hat jaye)

});

ul.addEventListener("click",function(event){

    if(event.target.nodeName=="BUTTON"){
    let listItem = event.target.parentElement;
    listItem.remove();
    console.log("deleted");
    }
});